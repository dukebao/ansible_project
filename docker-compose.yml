---
version: '3.3'

services:
  db: 
    image: mysql
    env_file:
      - .env
    volumes:
      - ./databaseinit:/docker-entrypoint-initdb.d
      - ./mysqlpresistentdata:/var/lib/mysql
    ports:
      - "3306:3306"

  backend:
    build: backend
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      - db
    restart: unless-stopped

  frontend:
    build: frontend
    env_file:
      - .env
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped
